<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pro.core.repository.UserRepository">
  <select id="findAll" resultType="com.pro.core.entity.User">
    SELECT *
    FROM `enrich`.`user`
  </select>
  <insert id="createUser">
    insert into `enrich`.`user` (id, full_name, email, phone_number, address)
    values('1', 'thuan ha cong', 'adasda@gmail.com', '9630', 'kim hoa');
  </insert>
</mapper>
